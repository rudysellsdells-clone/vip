# Navigation Patch

Add this item under the `Revenue` nav group in:

```text
src/components/layout/SidebarNav.tsx
```

```ts
{ label: "Link Builder", href: "/link-builder" },
```

I did not include a full `SidebarNav.tsx` replacement in this patch to avoid overwriting any recent navigation changes.
