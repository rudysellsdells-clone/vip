# Publishing readiness / Zapier action keys
# Gmail draft is known from the current setup.
ZAPIER_GMAIL_APP=gmail
ZAPIER_GMAIL_CREATE_DRAFT_ACTION=draft_v2

# Set these only after confirming the exact enabled Zapier MCP action keys.
ZAPIER_LINKEDIN_APP=linkedin
ZAPIER_LINKEDIN_CREATE_POST_ACTION=

ZAPIER_FACEBOOK_APP=facebook_pages
ZAPIER_FACEBOOK_CREATE_POST_ACTION=

# GalaxyAI publishing readiness
GALAXYAI_MEDIA_REQUEST_ACTION=manual_media_request

# H1.5A OpenAI visual asset generation
# OPENAI_API_KEY is already used by content generation.
# Optional overrides for image creation:
OPENAI_IMAGE_MODEL=gpt-image-1
OPENAI_IMAGE_SIZE=1024x1024
OPENAI_IMAGE_WIDE_SIZE=1024x1024
OPENAI_IMAGE_QUALITY=

# H1.8A Campaign Intelligence
# H1.8A is enabled by default and requires no additional user form fields.
# Emergency rollback only: set to 1 to restore the legacy all-memory prompt path.
VIP_DISABLE_CAMPAIGN_INTELLIGENCE=0

# H1.9 Strategy Engine V2
# Canonical campaign brief, source authority, whole-spine validation, and
# deterministic safeguards are enabled by default. Optional overrides:
# OPENAI_STRATEGY_MODEL=gpt-4.1-mini
# OPENAI_STRATEGY_QUALITY_MODEL=gpt-4.1
# OPENAI_ASSET_PACK_MODEL=gpt-4.1-mini
# VIP_STRATEGY_OPENAI_TIMEOUT_MS=30000
