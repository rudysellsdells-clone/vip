# H1.4D Dashboard Account Scope Fix

Apply this ZIP over the current H1.4C/H1.4C2/H1.4C3 build.

This patch fixes the dashboard leak where account users could see wrong-workspace items in **Needs your attention**.

No SQL is required.
