# H1.4C2 Testing Controls Patch

Apply this ZIP over H1.4C.

This patch adds:

- visible Sign out button
- `/api/auth/sign-out` route
- clearer Active workspace selector
- MASTER workspace selector helper text
- Use Workspace button on account cards
- Set Active Workspace button on account detail pages
- the prior H1.4C implicit-any build fix for campaign generation

No new SQL is included.
